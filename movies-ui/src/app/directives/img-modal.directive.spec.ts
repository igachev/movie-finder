import { ImgModalDirective } from './img-modal.directive';

describe('ImgModalDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgModalDirective();
    expect(directive).toBeTruthy();
  });
});
